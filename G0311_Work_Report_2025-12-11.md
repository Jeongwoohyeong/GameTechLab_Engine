# G0311 작업 내역 보고서

**작성일:** 2025-12-11
**작업자:** g0311 (gsc0815@naver.com)
**브랜치:** fix/refactor-all

---

## 커밋 요약

| 시간 | 커밋 해시 | 내용 |
|------|-----------|------|
| 17:44 | 9893321 | fix: propertys |
| 16:51 | 8746fcc | fix: knife 정상화 |
| 15:43 | abed4f5 | fix: default property |
| 15:14 | 3f19879 | fix: toon shader cbuffer slot bug |

---

## 상세 작업 내역

### 1. fix: propertys (9893321) - 17:44

**수정 파일:**
- `AngryCoachCharacter.cpp`
- `AngryCoachPlayerController.cpp`
- `AngryCoachPlayerController.h`
- `ConsoleWidget.cpp`

#### 1-1. 자유 카메라 모드 추가
F1 키로 토글 가능한 자유 카메라 모드를 구현했습니다.

```cpp
// AngryCoachPlayerController.h
// 자유 카메라 모드
bool bFreeCameraMode = false;
float FreeCameraMoveSpeed = 10.0f;
float FreeCameraRotateSpeed = 50.0f;
```

```cpp
// AngryCoachPlayerController.cpp
// F1 키로 자유 카메라 모드 토글
if (InputManager.IsKeyPressed(VK_F1))
{
    bFreeCameraMode = !bFreeCameraMode;

    // 자유 카메라 모드 끄면 기본 회전으로 복원
    if (!bFreeCameraMode && GameCamera)
    {
        GameCamera->SetActorRotation(FVector{0.f, 30.f, 0.f});
    }
}
```

#### 1-2. 자유 카메라 WASD + 마우스 조작 구현

```cpp
// WASD로 카메라 이동
if (InputManager.IsKeyDown('W')) CamMove.X += 1.0f;  // 전진
if (InputManager.IsKeyDown('S')) CamMove.X -= 1.0f;  // 후진
if (InputManager.IsKeyDown('A')) CamMove.Y -= 1.0f;  // 좌
if (InputManager.IsKeyDown('D')) CamMove.Y += 1.0f;  // 우
if (InputManager.IsKeyDown('Q')) CamMove.Z -= 1.0f;  // 하강
if (InputManager.IsKeyDown('E')) CamMove.Z += 1.0f;  // 상승

// 마우스로 카메라 회전
FVector2D MouseDelta = InputManager.GetMouseDelta();
if (MouseDelta.X != 0.0f || MouseDelta.Y != 0.0f)
{
    FVector CurrentEuler = GameCamera->GetActorRotation().ToEulerZYXDeg();
    CurrentEuler.Z += MouseDelta.X * FreeCameraRotateSpeed * DeltaTime;  // Yaw
    CurrentEuler.Y += MouseDelta.Y * FreeCameraRotateSpeed * DeltaTime;  // Pitch
    CurrentEuler.Y = FMath::Clamp(CurrentEuler.Y, -89.0f, 89.0f);  // Pitch 제한
    GameCamera->SetActorRotation(FVector(CurrentEuler.X, CurrentEuler.Y, CurrentEuler.Z));
}
```

#### 1-3. 에러 로그 콘솔 자동 오픈 비활성화

```cpp
// ConsoleWidget.cpp
// 에러 로그 발생시 콘솔창 자동 오픈 비활성화
// if (strstr(buf, "[error]") != nullptr)
// {
//     USlateManager::GetInstance().ForceOpenConsole();
// }
```

#### 1-4. 데칼 쿨타임 체크 비활성화

```cpp
// AngryCoachCharacter.cpp
// 너무 빠르게 다시 사용할 때
// if (LastDecalTime[0] < DecalMinInterval[0])
// {
//     return;
// }
```

---

### 2. fix: knife 정상화 (8746fcc) - 16:51

**수정 파일:**
- `GorillaHeavyAttackSkill.cpp`
- `GorillaLightAttackSkill.cpp`
- `KnifeAccessoryActor.cpp`

#### 2-1. 공격 스킬에 베이스 이펙트 파티클 재생 추가

```cpp
// GorillaHeavyAttackSkill.cpp, GorillaLightAttackSkill.cpp
// 베이스 이펙트 파티클 재생
if (SourceAccessory->BaseEffectParticle)
{
    SourceAccessory->BaseEffectParticle->ResetAndActivate();
}
```

#### 2-2. Knife 충돌 설정 수정

```cpp
// KnifeAccessoryActor.cpp - 생성자
AttackShape = Cast<UBoxComponent>(CreateAttackShape<UBoxComponent>(FName("AttackShape")));
AttackShape->SetGenerateOverlapEvents(false);
AttackShape->SetBlockComponent(false);
AttackShape->bOverrideCollisionSetting = true;
AttackShape->CollisionEnabled = ECollisionState::QueryOnly;
```

#### 2-3. Serialize/DuplicateSubObjects에서 AttackShape 포인터 복구

```cpp
// KnifeAccessoryActor.cpp
// AttackShape 포인터 복구
AttackShape = nullptr;
for (UShapeComponent* Shape : AttackShapes)
{
    if (UBoxComponent* Box = Cast<UBoxComponent>(Shape))
    {
        AttackShape = Box;
        AttackShape->bOverrideCollisionSetting = true;
        AttackShape->CollisionEnabled = ECollisionState::QueryOnly;
        break;
    }
}
```

---

### 3. fix: default property (abed4f5) - 15:43

**수정 파일:**
- `AngryCoachGameMode.cpp`
- `AngryCoachPlayerController.cpp`
- `GorillaAccessoryActor.cpp`
- `CamMod_DOF.h`

#### 3-1. 플레이어 스폰 위치 Z값 수정 (0 → 2)

```cpp
// AngryCoachGameMode.cpp
Player1->SetActorLocation(FVector(0, -15, 2));
Player2->SetActorLocation(FVector(0, 15, 2));
```

#### 3-2. DOF 파라미터 기본값 조정

```cpp
// CamMod_DOF.h
float FocalDistance = 20.0f;             // m (초점 거리) - 기존 16.0f
float FocalRegion = 50.0f;               // m (완전 선명 영역) - 기존 18.0f
float NearTransitionRegion = 10.0f;      // m (근경 블러 전환) - 기존 4.0f
float FarTransitionRegion = 60.0f;       // m (원경 블러 전환) - 기존 20.0f
float MaxNearBlurSize = 4.0f;            // pixels - 기존 6.0f
float MaxFarBlurSize = 4.0f;             // pixels - 기존 6.0f
```

#### 3-3. 동적 DOF 계산 조정

```cpp
// AngryCoachPlayerController.cpp
float DofFocalRegion = FMath::Max(30.f, (MaxPlayerDistToCam - MinPlayerDistToCam) * 3.f);
float DofNearTransition = 10.0f;   // 기존 0.1f
float DofFarTransition = 50.0f;    // 기존 0.5f
float DofMaxNearBlur = 3.0f / FMath::Max(1.0f, ZoomFactor);  // 기존 2.0f
float DofMaxFarBlur = 3.0f / FMath::Max(1.0f, ZoomFactor);   // 기존 2.0f
```

#### 3-4. 고릴라 폼 전환 시 충돌 설정 추가

```cpp
// GorillaAccessoryActor.cpp
CharacterMesh->SetCollisionEnabled(ECollisionState::QueryOnly);  // 물리 충돌 없이 오버랩만
```

---

### 4. fix: toon shader cbuffer slot bug (3f19879) - 15:14

**수정 파일:**
- `UberLit.hlsl`
- `AngryCoachCharacter.cpp`
- `AngryCoachGameMode.cpp`
- `Character.h`
- `ConstantBufferType.h`

#### 4-1. Toon Shader CBuffer 슬롯 변경 (b6 → b13)

```hlsl
// UberLit.hlsl
#if USE_CARTOON_SHADING
cbuffer CartoonParamsBuffer : register(b13)  // 기존 b6
{
    float CartoonOutlineThreshold;
    int CartoonShadingLevels;
    // ...
}
```

```cpp
// ConstantBufferType.h
CONSTANT_BUFFER_INFO(FCartoonParamsBufferType, 13, false, true)  // b13, 기존 b6
```

#### 4-2. 공격 데미지 밸런싱

```cpp
// AngryCoachCharacter.cpp
// 점프 공격
BaseDamage = 10.0f;   // 기존 8.0f

// 약공격
BaseDamage = 6.0f;    // 기존 5.0f

// 강공격
BaseDamage = 12.0f;   // 기존 10.0f

// 스킬
BaseDamage = 18.0f;   // 기존 15.0f
```

#### 4-3. 캐릭터 최대 체력 증가

```cpp
// Character.h
float MaxHealth = 100.0f;  // 기존 20.0f
```

#### 4-4. 플레이어 스폰 위치/회전 조정

```cpp
// AngryCoachGameMode.cpp
Player1->SetActorLocation(FVector(0, -15, 0));  // 기존 (0, -2.5f, 2)
Player1->SetActorRotation(FVector(0, 0, 90));   // Player2를 향함

Player2->SetActorLocation(FVector(0, 15, 0));   // 기존 (0, 2.5f, 2)
Player2->SetActorRotation(FVector(0, 0, -90));  // Player1을 향함
```

---

## 작업 요약

| 카테고리 | 작업 항목 |
|----------|-----------|
| 기능 추가 | 자유 카메라 모드 (F1 토글, WASD+QE+마우스) |
| 버그 수정 | Toon Shader CBuffer 슬롯 충돌 해결 (b6→b13) |
| 버그 수정 | Knife AttackShape 충돌 설정 및 포인터 복구 |
| 버그 수정 | 고릴라 폼 전환 시 충돌 설정 누락 수정 |
| 밸런스 | 공격 데미지 상향 (약공 5→6, 강공 10→12, 스킬 15→18, 점프 8→10) |
| 밸런스 | 캐릭터 최대 체력 증가 (20→100) |
| 카메라 | DOF 파라미터 조정 (선명 영역 확대, 블러 완화) |
| 기타 | 에러 로그 콘솔 자동 오픈 비활성화 |
| 기타 | 플레이어 스폰 위치/회전 조정 |
